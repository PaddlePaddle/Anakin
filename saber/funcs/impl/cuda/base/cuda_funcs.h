#include <stdio.h>
#include "saber/core/tensor.h"

void my_print ();

/*void print_gpu_data(anakin::saber::Tensor<4, RTCUDA, float, NCHW>& tensor , \
    int index = 0);

void set_gpu_data(anakin::saber::Tensor<4, RTCUDA, float, NCHW>& tensor, \
    float data, anakin::saber::Shape<4> shape);

void sum_tensor_gpu(anakin::saber::Tensor<4, RTCUDA, float, NCHW>& tensor, float* sum);*/
